{
  "name": "telegram-marketplace-checkout",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@marketplace/api",
    "dev:web": "npm run dev --workspace=@marketplace/web",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace=@marketplace/api"
  },
  "engines": {
    "node": ">=20"
  }
}
